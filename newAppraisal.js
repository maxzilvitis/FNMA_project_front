import React from 'react';
import { StyleSheet, Text, View, FlatList, ActivityIndicator } from 'react-native';

export default class NewAppraisal extends React.Component {
    render() {
        return (
            <Text>This is where we add a new appraisal</Text>
        );
    }
}
